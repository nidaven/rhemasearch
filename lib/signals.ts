import { signal } from "@preact/signals-react";

export const urlState = signal('');
export const timestampState = signal(0.0);
export const imageUrlState = signal('');
export const titleState = signal('');
export const loadedState = signal(false);

